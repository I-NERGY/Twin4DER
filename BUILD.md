# Sytem setup with docker compose
## Development system

Build docker images and setup system:

```
docker compose up --build
```

In case of errors, cancel (Ctrl + C) and restart:
```
docker compose up
```