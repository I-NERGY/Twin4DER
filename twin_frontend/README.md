# Build and run
## Development
### Locally
Build and run locally in development mode:
```
npm install
npm start
```

### Using Docker
Build docker container and run it with docker compose:
```
docker compose up
```

## Production